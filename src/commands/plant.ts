// Removed — seeds are only available via /seed-drop (no open planting).
// File kept to avoid breaking any cached imports; not registered in commands/index.ts.
export const data = { name: "plant", description: "disabled", options: [] };
export async function execute() {}
